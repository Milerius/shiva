if (SHIVA_USE_SFML_AS_RENDERER)
    set(SFML_STATIC_LIBRARIES TRUE)
    find_package(SFML CONFIG REQUIRED system window graphics audio)
    include(shiva/sfml/graphics/CMakeSources.cmake)
    set(MODULE_PATH ${CMAKE_CURRENT_SOURCE_DIR})
    CREATE_MODULE_PLUGIN(shiva::system-sfml-graphics "${MODULE_SOURCES}" ${MODULE_PATH} "systems")
    target_link_libraries(system-sfml-graphics PUBLIC shiva::ecs sfml-graphics)
    AUTO_TARGETS_MODULE_INSTALL(system-sfml-graphics)
endif ()